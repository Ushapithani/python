const details = {
    name: "usha",
    branch: "AIML",
    year: 3
};

for (let key in car) {
    console.log(`${key}: ${details[key]}`);
}





const a = [1, 2, 3, 4, 5];

for (const i in a) {
  	console.log(a[i]);
}
